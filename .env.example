# Copy this file to .env and fill in your values.
# .env is gitignored — never commit it.

# Monday.com API key
# Get it from: https://monday.com > Profile > Developer > API v2 Token
MONDAY_API_KEY=your_monday_api_key_here
